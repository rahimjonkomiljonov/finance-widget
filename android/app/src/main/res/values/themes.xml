<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window theme only (shown before Compose draws); the UI itself is themed in Compose. -->
    <style name="Theme.FinanceWidget" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#F6F6F8</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
